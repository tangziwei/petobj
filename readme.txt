调用此函数需要在当前路径下有一张名叫lena.jpg的图像
然后运行
python 用到的库有 opencv numpy matplotlib scipy
linlux(ubuntu) 下在终端安装这几个库的指令为(需要安装pip)

pip install numpy
pip install matplotlib
pip install scipy
pip install cv2  (还是pip install opencv-python?有点记不清楚了)

由于模拟投影过程的函数没有优化,调用时间可能比较久
